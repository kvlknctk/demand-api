version: '3'

services:
  node-app:
    container_name: demand-app
    command: yarn start
    volumes:
      - .:/usr/src/demand-app
      - "/etc/timezone:/etc/timezone:ro"
      - "/etc/localtime:/etc/localtime:ro"
